<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    android:title="@string/settings"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <Preference
        android:key="security_settings"
        android:icon="@drawable/ic_settings_security"
        android:title="@string/security_category" />

    <Preference
        android:key="forwarding_settings"
        android:icon="@drawable/ic_settings_forward_notifications"
        android:title="@string/notification_forwarding_category" />

    <Preference
        android:key="notification_sender"
        android:title="Send notification" />

</PreferenceScreen>