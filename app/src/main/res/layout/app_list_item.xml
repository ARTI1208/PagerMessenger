<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?selectableItemBackground"
    android:minHeight="@dimen/chat_list_item_height"
    android:paddingHorizontal="@dimen/list_item_horizontal_padding">

    <ImageView
        android:id="@+id/app_logo"
        android:layout_width="@dimen/chat_list_item_height"
        android:layout_height="@dimen/chat_list_item_height"
        android:padding="@dimen/app_icon_padding" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="@dimen/chat_list_item_height"
        android:layout_marginStart="10dp"
        android:layout_weight="1"
        android:gravity="center_vertical"
        android:orientation="vertical">

        <TextView
            android:id="@+id/app_name"
            style="@style/AppListItemTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxLines="1"
            tools:text="Some app" />

        <TextView
            android:id="@+id/app_package"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            tools:text="Some package" />
    </LinearLayout>

    <CheckBox
        android:id="@+id/item_select_check_box"
        android:layout_width="wrap_content"
        android:layout_height="match_parent" />

</LinearLayout>